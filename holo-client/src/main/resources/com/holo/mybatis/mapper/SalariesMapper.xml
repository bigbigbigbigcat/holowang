<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.holo.springboot.holoclient.mapper.SalariesMapper">
    <resultMap id="BaseResultMap" type="com.holo.springboot.holoclient.pojo.Salaries">
        <id column="emp_no" jdbcType="INTEGER" property="empNo"/>
        <id column="from_date" jdbcType="DATE" property="fromDate"/>
        <result column="salary" jdbcType="INTEGER" property="salary"/>
        <result column="to_date" jdbcType="DATE" property="toDate"/>
    </resultMap>
    <delete id="deleteByPrimaryKey" parameterType="map">
        delete from salaries
        where emp_no = #{empNo,jdbcType=INTEGER}
        and from_date = #{fromDate,jdbcType=DATE}
    </delete>
    <insert id="insert" parameterType="com.holo.springboot.holoclient.pojo.Salaries">
        insert into salaries (emp_no, from_date, salary,
        to_date)
        values (#{empNo,jdbcType=INTEGER}, #{fromDate,jdbcType=DATE}, #{salary,jdbcType=INTEGER},
        #{toDate,jdbcType=DATE})
    </insert>
    <update id="updateByPrimaryKey" parameterType="com.holo.springboot.holoclient.pojo.Salaries">
        update salaries
        set salary = #{salary,jdbcType=INTEGER},
        to_date = #{toDate,jdbcType=DATE}
        where emp_no = #{empNo,jdbcType=INTEGER}
        and from_date = #{fromDate,jdbcType=DATE}
    </update>
    <select id="selectByPrimaryKey" parameterType="map" resultMap="BaseResultMap">
        select emp_no, from_date, salary, to_date
        from salaries
        where emp_no = #{empNo,jdbcType=INTEGER}
        and from_date = #{fromDate,jdbcType=DATE}
    </select>
    <select id="selectAll" resultMap="BaseResultMap">
        select emp_no, from_date, salary, to_date
        from salaries
    </select>
</mapper>