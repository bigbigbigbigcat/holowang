<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.holo.springboot.holoclient.mapper.TitlesMapper">
    <resultMap id="BaseResultMap" type="com.holo.springboot.holoclient.pojo.Titles">
        <id column="emp_no" jdbcType="INTEGER" property="empNo"/>
        <id column="title" jdbcType="VARCHAR" property="title"/>
        <id column="from_date" jdbcType="DATE" property="fromDate"/>
        <result column="to_date" jdbcType="DATE" property="toDate"/>
    </resultMap>
    <delete id="deleteByPrimaryKey" parameterType="map">
        delete from titles
        where emp_no = #{empNo,jdbcType=INTEGER}
        and title = #{title,jdbcType=VARCHAR}
        and from_date = #{fromDate,jdbcType=DATE}
    </delete>
    <insert id="insert" parameterType="com.holo.springboot.holoclient.pojo.Titles">
        insert into titles (emp_no, title, from_date,
        to_date)
        values (#{empNo,jdbcType=INTEGER}, #{title,jdbcType=VARCHAR}, #{fromDate,jdbcType=DATE},
        #{toDate,jdbcType=DATE})
    </insert>
    <update id="updateByPrimaryKey" parameterType="com.holo.springboot.holoclient.pojo.Titles">
        update titles
        set to_date = #{toDate,jdbcType=DATE}
        where emp_no = #{empNo,jdbcType=INTEGER}
        and title = #{title,jdbcType=VARCHAR}
        and from_date = #{fromDate,jdbcType=DATE}
    </update>
    <select id="selectByPrimaryKey" parameterType="map" resultMap="BaseResultMap">
        select emp_no, title, from_date, to_date
        from titles
        where emp_no = #{empNo,jdbcType=INTEGER}
        and title = #{title,jdbcType=VARCHAR}
        and from_date = #{fromDate,jdbcType=DATE}
    </select>
    <select id="selectAll" resultMap="BaseResultMap">
        select emp_no, title, from_date, to_date
        from titles
    </select>
</mapper>